version: '3'

services:
  postgres:
    image: postgres
    ports:
     - 5432:5432
    environment:
     POSTGRES_PASSWORD: root
    volumes:
     - /home/vagrant/docker/data/postgres:/var/lib/postgresql

